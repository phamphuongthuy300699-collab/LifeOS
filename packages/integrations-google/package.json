{
  "name": "@lifeos/integrations-google",
  "version": "0.0.1",
  "private": true,
  "description": "Gmail + Google Calendar adapter",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@lifeos/shared": "workspace:*",
    "googleapis": "^171.4.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}

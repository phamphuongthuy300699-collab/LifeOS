{
  "name": "@lifeos/config",
  "version": "0.0.1",
  "private": true,
  "description": "Shared configuration: tsconfig, env schema",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}

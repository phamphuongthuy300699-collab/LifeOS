{
  "name": "@lifeos/domain-core",
  "version": "0.0.1",
  "private": true,
  "description": "Shared domain types, base entities, policies",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@lifeos/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}

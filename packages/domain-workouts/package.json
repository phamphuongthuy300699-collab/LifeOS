{
  "name": "@lifeos/domain-workouts",
  "version": "0.0.1",
  "private": true,
  "description": "Workout tracking domain",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@lifeos/shared": "workspace:*",
    "@lifeos/domain-core": "workspace:*",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}

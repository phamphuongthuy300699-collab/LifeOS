/**
 * LifeOS Worker — Background job processor.
 *
 * Will be wired with BullMQ in Sprint 3 (mail sync).
 * For now, this is a skeleton to validate monorepo builds.
 */

console.log('🔧 LifeOS Worker starting...');
console.log('⏳ No job processors registered yet. Waiting for Sprint 3.');

// Keep process alive
setInterval(() => {}, 60_000);
